import { TwoDirective } from './two.directive';

describe('TwoDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoDirective();
    expect(directive).toBeTruthy();
  });
});
